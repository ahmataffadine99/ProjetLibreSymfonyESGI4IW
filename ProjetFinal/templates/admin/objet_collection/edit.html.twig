{% extends 'admin/base.html.twig' %}

{% block title %}Modifier l'Objet{% endblock %}

{% block content %}
    <div class="container mx-auto py-8">
        <h1 class="text-3xl font-bold mb-6">Modifier l'Objet</h1>

        {{ form_start(form, {'attr': {'class': 'space-y-6'}}) }}
            {{ form_widget(form, {'attr': {'class': 'block w-full px-4 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500'}}) }}
            <button type="submit" class="inline-flex items-center px-6 py-3 border border-transparent text-base font-medium rounded-md shadow-sm text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 transition duration-300 ease-in-out">
                Enregistrer les modifications
            </button>
        {{ form_end(form) }}

        <div class="mt-8">
            <a href="{{ path('app_objets_list') }}" class="inline-flex items-center px-6 py-3 border border-gray-300 text-base font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 transition duration-300 ease-in-out">
                Retour à la liste
            </a>
        </div>
    </div>
{% endblock %}